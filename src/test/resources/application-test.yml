spring:
  data:
    redis:
      host: localhost
      port: 6379
      password: 
      timeout: 1000ms

  cache:
    type: redis
    redis:
      time-to-live: 60s

logging:
  level:
    com.example.springrediscrac: DEBUG
    org.springframework.test: DEBUG
    org.testcontainers: INFO